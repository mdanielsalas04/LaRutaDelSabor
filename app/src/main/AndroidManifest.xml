<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LaRutaDelSabor">

        <!-- LoginActivity -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RegisterActivity -->
        <activity
            android:name=".auth.RegisterActivity"
            android:exported="false" />

        <!-- HomeActivity -->
        <activity
            android:name=".tabs.HomeActivity"
            android:exported="false" />

        <!-- DetalleRutaActivity -->
        <activity
            android:name=".detalle.DetalleRutaActivity"
            android:exported="false" />

        <!-- DetalleSubrutaActivity -->
        <activity
            android:name=".detalle.DetalleSubrutaActivity"
            android:exported="false" />

        <!-- DetalleFeriaActivity -->
        <activity
            android:name=".detalle.DetalleFeriaActivity"
            android:exported="false" />

    </application>

</manifest>