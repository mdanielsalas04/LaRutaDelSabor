<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LaRutaDelSabor">
        <activity
            android:name=".admin.pages.AdminRutasListActivity"
            android:exported="false" />
        <activity
            android:name=".admin.pages.AdminActivity"
            android:exported="false" />
        <activity
            android:name=".usuario.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".usuario.auth.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".usuario.tabs.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".usuario.detalle.DetalleRutaActivity"
            android:exported="false" />
        <activity
            android:name=".usuario.detalle.DetalleFeriaActivity"
            android:exported="false" />
        <activity android:name=".admin.pages.AdminFeriasListActivity" />
    </application>

</manifest>